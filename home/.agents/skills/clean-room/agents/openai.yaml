interface:
  display_name: "clean-room"
  short_description: "Get a decision from an independent agent"
  default_prompt: "Use $clean-room to get an independent decision from a fresh agent with no access to this conversation."

policy:
  allow_implicit_invocation: false
